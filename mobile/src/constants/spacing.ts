/** Uniform spacing scale — use everywhere for consistent rhythm */
export const spacing = {
  /** Horizontal screen padding */
  screen: 20,
  /** Gap between major blocks on a screen */
  section: 24,
  /** Gap from section title → first item below */
  titleBelow: 12,
  /** Gap between items in a list */
  item: 12,
  /** Gap inside cards / grouped content */
  inner: 8,
} as const;
